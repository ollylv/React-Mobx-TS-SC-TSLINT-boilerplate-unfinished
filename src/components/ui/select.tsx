import styled from 'styled-components';

const Select = styled.div`
  display: block;
  margin: 12px 6px;

  select {
    width: 160px;
    border: none;
    height: 40px;
    padding-left: 6px;
  }
`;

export default Select;
