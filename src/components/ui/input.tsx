import styled from 'styled-components';

const Input = styled.div`
  margin: 12px 6px;
  display: block;

  input {
    width: 180px;
    height: 40px;
    padding-left: 6px;
    border: none;
  }
`;

export default Input;
